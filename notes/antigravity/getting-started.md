<!--
- [INPUT]: 依赖 notes/antigravity/CLAUDE.md 的模块定位与索引
- [OUTPUT]: 输出 快速开始 文档
- [POS]: 位于 notes/antigravity 模块的 快速开始 笔记
- [PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md
-->

# 快速开始

## 安装

### 系统要求

- macOS 12+ / Windows 10+ / Ubuntu 20.04+
- 8GB RAM（推荐 16GB）
- Google 账号

### 下载安装

1. 访问 [antigravity.google](https://antigravity.google)
2. 选择操作系统版本下载
3. 安装并启动

### 首次设置

1. 使用 Google 账号登录
2. 选择主题和偏好设置
3. 可选：导入现有项目

---

## 创建第一个项目

### Step 1: 新建项目

1. 打开 Antigravity
2. 选择 `File > New Project` 或使用 Agent Manager
3. 选择空文件夹作为项目目录

### Step 2: 打开 Agent Manager

- 点击侧边栏的 Agent 图标
- 或使用快捷键打开

### Step 3: 创建任务

1. 在 Agent Manager 中点击 `New Task`
2. 输入自然语言描述：

```
创建一个 React + TypeScript 的待办事项应用：
- 添加/删除/完成任务
- 本地存储持久化
- 响应式设计
- 使用 TailwindCSS
```

### Step 4: 观察 Agent 工作

Agent 会自动：

1. 📋 分析需求，创建 `task.md`
2. 📝 生成 `implementation_plan.md`
3. 🛠️ 创建项目结构和代码
4. 📦 安装依赖
5. 🧪 运行和测试
6. 📄 生成 `walkthrough.md`

### Step 5: 审查和迭代

- 查看生成的代码
- 在浏览器中预览应用
- 提出修改需求继续迭代

---

## 基础操作

### 常用快捷键

| 快捷键                 | 功能               |
| ---------------------- | ------------------ |
| `Cmd/Ctrl + Shift + A` | 打开 Agent Manager |
| `Cmd/Ctrl + Enter`     | 发送提示词         |
| `Cmd/Ctrl + .`         | 接受建议           |
| `Esc`                  | 取消当前操作       |

### 与 Agent 对话

```
# 简单请求
添加一个深色模式切换按钮

# 详细请求
重构 Header 组件：
- 提取导航逻辑到 useNavigation hook
- 添加响应式菜单
- 保持现有 API 不变
```

### 文件引用

使用 `@` 引用文件提供上下文：

```
根据 @src/components/Button.tsx 的风格
创建一个新的 Card 组件
```

---

## 开发模式选择

### Agent-driven（自动驾驶）

```
设置 > Development Mode > Agent-driven
```

Agent 自动执行所有操作，适合：

- 新项目快速启动
- 明确需求的功能开发

### Review-driven（审核模式）

```
设置 > Development Mode > Review-driven
```

每个操作需要确认，适合：

- 敏感代码修改
- 学习 Agent 的决策过程

### Agent-assisted（推荐）

```
设置 > Development Mode > Agent-assisted
```

用户控制 + AI 辅助，适合：

- 日常开发工作
- 需要人工判断的场景

---

## 下一步

- [开发模式详解](./development-modes.md)
- [Artifacts 系统](./artifacts.md)
- [最佳实践](./best-practices.md)
