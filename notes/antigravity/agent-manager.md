# Agent Manager 详解

Agent Manager 是 Antigravity 的核心控制中心，用于创建、管理和监控 AI Agent。

---

## 概述

Agent Manager 不仅仅是一个聊天界面，而是一个完整的任务编排系统：

```
┌─────────────────────────────────────────────────────┐
│                  Agent Manager                       │
├───────────────────┬─────────────────────────────────┤
│                   │                                  │
│   Workspace       │        Task Thread              │
│   Navigator       │                                  │
│                   │   ┌─────────────────────────┐   │
│   📁 Project A    │   │  🤖 Agent Activity Log   │   │
│   📁 Project B    │   │                         │   │
│   📁 Project C    │   │  💭 Thought Process     │   │
│                   │   │                         │   │
│                   │   │  📋 Execution Plan      │   │
│   ───────────────│   │                         │   │
│                   │   │  ✅ Completed Steps     │   │
│   Active Agents   │   │                         │   │
│   🤖 Agent 1      │   │  📄 Artifacts           │   │
│   🤖 Agent 2      │   │                         │   │
│   🤖 Agent 3      │   └─────────────────────────┘   │
│                   │                                  │
└───────────────────┴─────────────────────────────────┘
```

---

## 核心功能

### 1. 任务创建

**快速创建：**

1. 打开 Agent Manager (`Cmd+Shift+A`)
2. 选择目标工作区
3. 点击 "New Task"
4. 输入自然语言描述

**高级创建：**

```
[设置]
模式: Agent-assisted
模型: Gemini 3 Pro
规则: @.agent/rules/coding-style.md
工作流: @.agent/workflows/new-feature.md

[任务]
创建用户认证模块...
```

### 2. Agent 监控

每个 Agent 线程显示：

| 信息          | 说明                        |
| ------------- | --------------------------- |
| **状态**      | 运行中 / 等待 / 完成 / 错误 |
| **思维过程**  | Agent 的推理链              |
| **执行计划**  | 当前执行的步骤              |
| **活动日志**  | 所有操作记录                |
| **Artifacts** | 生成的工件                  |

### 3. 多 Agent 编排

同时运行多个 Agent：

```
Agent 1 (主任务): 实现核心功能
    └── Agent 2 (子任务): 编写测试
    └── Agent 3 (子任务): 生成文档
```

### 4. 异步反馈

**执行中提供反馈：**

```
Agent 正在生成代码...

[用户评论] 请使用 Zustand 而不是 Context

Agent: 好的，我会调整状态管理方案...
```

---

## 界面详解

### 左侧：工作区导航

```
📁 Workspaces
├── frontend-app (3 agents)
│   ├── 🤖 Feature Dev (running)
│   ├── 🤖 Testing (idle)
│   └── 🤖 Code Review (done)
├── backend-api (1 agent)
│   └── 🤖 API Design (running)
└── + Add Workspace
```

### 右侧：任务详情

**思维过程面板：**

```
💭 Thought Process

1. 分析用户需求...
2. 检查现有代码结构...
3. 决定使用 React Hook Form...
4. 规划组件层次...
```

**执行计划面板：**

```
📋 Execution Plan

[x] 创建目录结构
[x] 定义 TypeScript 类型
[/] 实现表单组件
[ ] 添加验证逻辑
[ ] 编写测试
```

**Artifacts 面板：**

```
📄 Artifacts

├── task.md
├── implementation_plan.md
├── screenshots/
│   └── form-preview.png
└── walkthrough.md (pending)
```

---

## 操作指南

### 创建新任务

1. **选择工作区**
   - 点击左侧工作区
   - 或拖拽文件夹到 Agent Manager

2. **输入任务描述**

   ```
   创建用户登录功能：
   - 邮箱密码登录
   - 记住我功能
   - 忘记密码链接
   ```

3. **配置选项**（可选）
   - 选择开发模式
   - 指定使用的模型
   - 应用规则/工作流

4. **开始执行**
   - 点击 "Start" 或按 Enter

### 管理运行中的 Agent

**暂停/恢复：**

```
点击 Agent 条目 → Pause / Resume
```

**提供反馈：**

```
点击 Agent 输出 → Add Comment
```

**重定向任务：**

```
"停止当前方案，改用 Server Components 实现"
```

**终止任务：**

```
点击 Agent 条目 → Stop
```

### 查看 Agent 思维

1. 点击 Agent 条目
2. 展开 "Thought Process" 面板
3. 查看实时推理过程

### 管理 Artifacts

1. 点击 Artifacts 面板
2. 查看生成的工件
3. 点击预览或在编辑器中打开
4. 添加评论或请求修改

---

## Multi-Agent 用法

### 场景一：并行开发

```
Workspace: e-commerce

Agent 1: 开发商品列表页
Agent 2: 开发购物车功能
Agent 3: 开发结算流程
```

### 场景二：职责分离

```
Workspace: feature-auth

Agent 1 (Main): 实现核心认证逻辑
    → Agent 2 (Sub): 编写单元测试
    → Agent 3 (Sub): 生成 API 文档
```

### 场景三：代码审查

```
Workspace: project

Agent 1: 开发新功能
Agent 2: 实时代码审查
    - 发现问题立即反馈给 Agent 1
```

---

## 快捷操作

| 操作               | 快捷键        |
| ------------------ | ------------- |
| 打开 Agent Manager | `Cmd+Shift+A` |
| 新建任务           | `Cmd+N`       |
| 发送消息           | `Enter`       |
| 发送多行消息       | `Shift+Enter` |
| 暂停 Agent         | `Cmd+P`       |
| 停止 Agent         | `Cmd+.`       |

---

## 最佳实践

### 1. 任务粒度

**❌ 过大：**

```
创建完整的电商网站
```

**✅ 适中：**

```
创建商品列表页面，包含筛选和分页
```

### 2. 上下文提供

```
参考现有组件：
@src/components/ProductCard.tsx
@src/hooks/useProducts.ts

创建商品详情页面
```

### 3. 明确验收标准

```
创建登录表单：
- 邮箱格式验证
- 密码最少 8 位
- 登录成功跳转 /dashboard
- 错误显示在表单下方
```

### 4. 利用 Artifacts

定期检查生成的 Artifacts：

- `task.md`: 了解进度
- `implementation_plan.md`: 审核方案
- 截图: 验证 UI

### 5. 及时反馈

发现问题立即评论：

```
这里应该使用相对路径而不是绝对路径
```

---

## 常见问题

### Q: Agent 卡住了怎么办？

A: 尝试以下步骤：

1. 添加评论提供更多上下文
2. 暂停后重新描述需求
3. 停止并创建新任务

### Q: 多个 Agent 冲突怎么办？

A: 确保任务边界清晰，避免修改同一文件。使用 Git 分支隔离。

### Q: 如何查看历史任务？

A: 在 Agent Manager 左下角查看 "Task History"。
